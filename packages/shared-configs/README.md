# shared-configs

Shared DTOs and defaults used by frontend clients.

## Exports

- `ApiResponse<T>`, `Meta`, `ApiError`
- `Store`, `Product`, `Price`
- `DEFAULT_API_URL`

## Notes

Backend types are the source of truth; these are kept in sync for frontend use.
